} else {
cat("\n✅ Success: Parser and Standard results are identical (stringent check).\n")
}
# What contrast does res_parser correspond to?
metadata(res_parser)$contrast
# or
attr(res_parser, "contrast")
# Print it to confirm positions
contrast_vec <- setNames(rep(0, length(resultsNames(dds_standard))),
resultsNames(dds_standard))
contrast_vec["ConditionWT.Treatment4Gy"] <- -1
contrast_vec["Cell_LineARCaPM.ConditionWT.Treatment4Gy"] <- -1
print(contrast_vec)
summary(res_standard_ARCaPM)
hist(res_standard_ARCaPM$log2FoldChange, breaks=50)
hist(res_parser$log2FoldChange, breaks=50)
summary(res_parser)
# Do the TOP genes at least agree in direction?
common_genes <- intersect(rownames(res_parser), rownames(res_standard_ARCaPM))
# Get top 100 by parser
top100_parser <- rownames(res_parser[order(res_parser$pvalue), ])[1:100]
# Check their direction in standard
dir_parser   <- sign(res_parser[top100_parser, "log2FoldChange"])
dir_standard <- sign(res_standard_ARCaPM[top100_parser, "log2FoldChange"])
table(dir_parser, dir_standard)
# What contrast string was used for res_parser?
metadata(res_parser)$contrast
# Rerun just the parser contrast vector calculation
design_vars <- all.vars(DESeq2::design(dds_parser))
mod_mat <- model.matrix(DESeq2::design(dds_parser),
data = colData(dds_parser))
df_groups <- colData(dds_parser) %>%
as.data.frame() %>%
tidyr::unite(col = "Groups", all_of(design_vars), sep = "_", remove = FALSE)
groups <- unique(df_groups$Groups)
group_coef_list <- lapply(groups, function(i)
colMeans(as.matrix(mod_mat[df_groups$Groups == i, , drop = FALSE])))
names(group_coef_list) <- groups
# Print all group names - do they match your contrast string exactly?
print(names(group_coef_list))
design(dds_parser)
contrast <- "(NDRG1_4Gy_22Rv1-NDRG1_0Gy_22Rv1)-(WT_4Gy_22Rv1-WT_0Gy_22Rv1)"
dds_parser <- fit_deseq2_model(expr_mat    = deseq2_data$expr_mat,
txi         = deseq2_data$txi,
metadata    = deseq2_data$metadata,
design      = "0+Comparisons")
dds_rds_path <- dds_parser
dds <- dds_rds_path
# For ashr
set.seed(1234)
# ---- 🧮 Dynamic Contrast Parsing (PARSER METHOD) ----
# Extract design variables (e.g., c("Condition", "Batch"))
# all.vars is robust; it handles ~Batch+Condition and ~Batch+Condition*Treatment
design_vars <- all.vars(DESeq2::design(dds))
# Extract the model matrix
# This is useful for downstream checking of rank deficiency
mod_mat <- stats::model.matrix(DESeq2::design(dds),
data = SummarizedExperiment::colData(dds))
# Create "Groups" column for easy subsetting/contrasts
if (length(design_vars) > 0) {
df_groups <- SummarizedExperiment::colData(dds) %>%
as.data.frame() %>%
tidyr::unite(col = "Groups", dplyr::all_of(design_vars), sep = "_", remove = FALSE)
# Update the dds colData to include this combined Groups column
SummarizedExperiment::colData(dds)$Groups <- as.factor(df_groups$Groups)
} else {
# If design is ~1, Groups is just "All"
df_groups <- SummarizedExperiment::colData(dds)$Groups <- as.factor("All")
}
# Define all possible groups that could be compared based on the design
groups <- unique(df_groups$Groups)
# Map groups to mean model coefficients (Get all possible coefficient vectors)
group_coef_list <- lapply(groups, function(i) colMeans(as.matrix(mod_mat[df_groups$Groups == i, , drop = FALSE])))
names(group_coef_list) <- groups
# Recursive function to evaluate the contrast string (e.g., "A-B") as a vector operation
replace_symbols <- function(node) {
if (is.symbol(node)) {
nm <- as.character(node)
if (nm %in% names(group_coef_list)) {
return(group_coef_list[[nm]])
} else {
# it's an operator like "-" or "+" → return unchanged
return(node)
}
} else if (is.call(node)) {
return(as.call(lapply(node, replace_symbols)))
} else {
return(node)
}
}
parsed_expr <- base::parse(text = contrast)[[1]]
expr_sub <- replace_symbols(parsed_expr)
contrast_parser <- base::eval(expr_sub)
# ⚠️ Sanity Check: contrast_parser Alignment
if (length(contrast_parser) != ncol(mod_mat)) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = glue::glue("Length of contrast_parser ({length(contrast_parser)}) does
not match the number of columns in the design matrix ({ncol(mod_mat)})."))
}
if (all(contrast_parser == 0)) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = "contrast_parser is all zeros — invalid contrast. Check the user-provided contrast string.")
}
missing_groups <- setdiff(all.vars(parsed_expr), names(group_coef_list))
if (length(missing_groups) > 0) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = glue::glue("These groups in contrast do not exist in the design: {paste(missing_groups, collapse=', ')}"))
}
if (any(is.na(contrast_parser))) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = "Contrast vector contains NA — possible missing or empty group.")
}
# ---- 🧮 Dynamic Contrast (STANDARD METHOD) ----
design_vars <- all.vars(design(dds))
condition_col <- tail(design_vars, 1)
all_contrasts <- DESeq2::resultsNames(dds)
terms <- all.vars(as.formula(paste0("~", contrast)))
if (length(terms) == 2){
# --- SIMPLE PAIRWISE (e.g., A - B) ---
contrast_standard <- c(condition_col, terms[1], terms[2])
} else {
# --- COMPLEX INTERACTION ((A-B) - (C-D)) ---
contrast_standard <- contrast_parser
}
# ---- 🧬 Results & LFC Shrinkage ----
res_parser <- DESeq2::results(object               = dds,
contrast             = contrast_parser,
lfcThreshold         = lfc_cutoff,
altHypothesis        = "greaterAbs",
cooksCutoff          = TRUE,
independentFiltering = TRUE,
alpha                = padj_cutoff,
pAdjustMethod        = "BH")
# Use 'ashr' for shrinkage as it is robust for varied effect sizes
res_parser <- DESeq2::lfcShrink(dds = dds, res = res_parser, type = "ashr", quiet = TRUE)
#summary(res_parser)
res_standard <- DESeq2::results(object             = dds,
contrast             = contrast_standard,
lfcThreshold         = lfc_cutoff,
altHypothesis        = "greaterAbs",
cooksCutoff          = TRUE,
independentFiltering = TRUE,
alpha                = padj_cutoff,
pAdjustMethod        = "BH")
# Use 'ashr' for shrinkage as it is robust for varied effect sizes
res_standard <- DESeq2::lfcShrink(dds = dds, res = res_standard, type = "ashr", quiet = TRUE)
#summary(res_standard)
# ---- 🔍 Detailed Comparison of Results ----
# 1. Convert to dataframes for comparison
df_p <- as.data.frame(res_parser)
df_s <- as.data.frame(res_standard)
# 2. Check rownames match
if (!all(rownames(df_p) == rownames(df_s))) {
stop("Row names do not match between Parser and Standard results!")
}
# 3. Check log2FoldChange within tight tolerance
tolerance <- 1e-8
lfc_diff <- abs(df_p$log2FoldChange - df_s$log2FoldChange)
lfc_is_equal <- all(lfc_diff < tolerance, na.rm = TRUE)
max_lfc_diff <- max(lfc_diff, na.rm = TRUE)
# 4. Check padj within tight tolerance
padj_diff <- abs(df_p$padj - df_s$padj)
padj_is_equal <- all(padj_diff < tolerance, na.rm = TRUE)
max_padj_diff <- max(padj_diff, na.rm = TRUE)
# 5. Compare DEG sets
deg_p <- rownames(df_p)[which(df_p$padj < padj_cutoff & abs(df_p$log2FoldChange) > lfc_cutoff)]
deg_s <- rownames(df_s)[which(df_s$padj < padj_cutoff & abs(df_s$log2FoldChange) > lfc_cutoff)]
shared_degs <- intersect(deg_p, deg_s)
only_parser <- setdiff(deg_p, deg_s)
only_standard <- setdiff(deg_s, deg_p)
# 6. Print Stringent Comparison Report
cat("\n--- ⚖️  Parser vs Standard Comparison Report (Stringent) ---\n")
cat("Numerical LFC Match: ", lfc_is_equal, " (Max Δ = ", formatC(max_lfc_diff, format='e', digits=2), ")\n", sep="")
cat("Numerical padj Match:", padj_is_equal, " (Max Δ = ", formatC(max_padj_diff, format='e', digits=2), ")\n", sep="")
cat("\nDEG Set Comparison:\n")
cat("- Shared DEGs:       ", length(shared_degs), "\n")
cat("- Only in Parser:    ", length(only_parser), "\n")
cat("- Only in Standard:  ", length(only_standard), "\n")
if (!lfc_is_equal | !padj_is_equal | length(only_parser) > 0 | length(only_standard) > 0) {
cat("\n⚠️  Warning: Differences detected! Check p-value or LFC precision.\n")
} else {
cat("\n✅ Success: Parser and Standard results are identical (stringent check).\n")
}
summary(res_parser)
design(res_parser)
design(dds_parser)
design(dds_standard)
resultsNames(dds_parser)
resultsNames(dds_standard)
summary(res_parser)
summary(res_standard)
design(res_parser)
design(dds_parser)
dds_standard <- fit_deseq2_model(expr_mat    = deseq2_data$expr_mat,
txi         = deseq2_data$txi,
metadata    = deseq2_data$metadata,
design      = "Cell_Line * Condition * Treatment")
# 1. Extract and Shrink (Already done in your snippet)
contrast_vec <- setNames(rep(0, length(resultsNames(dds_standard))),
resultsNames(dds_standard))
# Fill in the terms you want
contrast_vec["ConditionWT.Treatment4Gy"] <- -1
contrast_vec["Cell_LineARCaPM.ConditionWT.Treatment4Gy"] <- -1
res_standard_ARCaPM <- results(dds_standard, contrast = contrast_vec)
design(res_parser)
design(dds_parser)
design(dds_standard)
resultsNames(dds_parser)
resultsNames(dds_standard)
summary(res_parser)
summary(res_standard_ARCaPM)
metadata(res_parser)$contrast
metadata(res_standard_ARCaPM)$contrast
# With ~0 + Comparisons, the model matrix columns ARE the groups directly
# So contrast is simply:
contrast_parser_15 <- setNames(rep(0, length(resultsNames(dds_parser))),
resultsNames(dds_parser))
contrast_parser_15["ComparisonsNDRG1_4Gy_ARCaPM"] <-  1
contrast_parser_15["ComparisonsNDRG1_0Gy_ARCaPM"] <- -1
contrast_parser_15["ComparisonsWT_4Gy_ARCaPM"]    <- -1
contrast_parser_15["ComparisonsWT_0Gy_ARCaPM"]    <-  1
print(contrast_parser_15)
# Get results explicitly
res_parser_15 <- DESeq2::results(dds_parser, contrast = contrast_parser_15, alpha = 0.1)
res_parser_15 <- DESeq2::lfcShrink(dds_parser, res = res_parser_15, type = "ashr", quiet = TRUE)
summary(res_parser_15)
cor(res_parser_15$log2FoldChange, res_standard_ARCaPM$log2FoldChange,
use = "complete.obs", method = "spearman")
summary(res_parser)
summary(res_parser_15)
summary(res_standard_ARCaPM)
print(contrast_parser)  # inside extract_deseq2_results for contrast 15
# Diagnose
parsed_expr <- base::parse(text = "(NDRG1_4Gy_ARCaPM-NDRG1_0Gy_ARCaPM)-(WT_4Gy_ARCaPM-WT_0Gy_ARCaPM)")[[1]]
print(parsed_expr)
all.vars(parsed_expr)
contrast
contrast <- contrasts[15]
contrast
metadata <- read.xlsx("C:/Users/kailasamms/OneDrive - Cedars-Sinai Health System/Desktop/Collaboration projects data/RNASeq_Human_Manish_22Rv1_ARCaPM/input/Manish_22Rv1_ARCaPM_metadata.xlsx")
txi <- readRDS("C:/Users/kailasamms/OneDrive - Cedars-Sinai Health System/Desktop/Collaboration projects data/RNASeq_Human_Manish_22Rv1_ARCaPM/Human_full/03.Salmon/Salmon_txi_Human_full.rds")
design
proj.params$deseq2$design
design      <- proj.params$deseq2$design
raw_counts_mat <- NULL
deseq2_data <- prepare_deseq2_input(expr_mat = raw_counts_mat,
txi      = txi,
metadata = metadata,
design   = design)
dds <- fit_deseq2_model(expr_mat    = deseq2_data$expr_mat,
txi         = deseq2_data$txi,
metadata    = deseq2_data$metadata,
design      = design)
# For ashr
set.seed(1234)
# ---- 🧮 Dynamic Contrast Parsing (PARSER METHOD) ----
# Extract design variables (e.g., c("Condition", "Batch"))
# all.vars is robust; it handles ~Batch+Condition and ~Batch+Condition*Treatment
design_vars <- all.vars(DESeq2::design(dds))
# Extract the model matrix
# This is useful for downstream checking of rank deficiency
mod_mat <- stats::model.matrix(DESeq2::design(dds),
data = SummarizedExperiment::colData(dds))
# Create "Groups" column for easy subsetting/contrasts
if (length(design_vars) > 0) {
df_groups <- SummarizedExperiment::colData(dds) %>%
as.data.frame() %>%
tidyr::unite(col = "Groups", dplyr::all_of(design_vars), sep = "_", remove = FALSE)
# Update the dds colData to include this combined Groups column
SummarizedExperiment::colData(dds)$Groups <- as.factor(df_groups$Groups)
} else {
# If design is ~1, Groups is just "All"
df_groups <- SummarizedExperiment::colData(dds)$Groups <- as.factor("All")
}
# Define all possible groups that could be compared based on the design
groups <- unique(df_groups$Groups)
# Map groups to mean model coefficients (Get all possible coefficient vectors)
group_coef_list <- lapply(groups, function(i) colMeans(as.matrix(mod_mat[df_groups$Groups == i, , drop = FALSE])))
names(group_coef_list) <- groups
# Recursive function to evaluate the contrast string (e.g., "A-B") as a vector operation
replace_symbols <- function(node) {
if (is.symbol(node)) {
nm <- as.character(node)
if (nm %in% names(group_coef_list)) {
return(group_coef_list[[nm]])
} else {
# it's an operator like "-" or "+" → return unchanged
return(node)
}
} else if (is.call(node)) {
return(as.call(lapply(node, replace_symbols)))
} else {
return(node)
}
}
parsed_expr <- base::parse(text = contrast)[[1]]
expr_sub <- replace_symbols(parsed_expr)
contrast_parser <- base::eval(expr_sub)
# ⚠️ Sanity Check: contrast_parser Alignment
if (length(contrast_parser) != ncol(mod_mat)) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = glue::glue("Length of contrast_parser ({length(contrast_parser)}) does
not match the number of columns in the design matrix ({ncol(mod_mat)})."))
}
if (all(contrast_parser == 0)) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = "contrast_parser is all zeros — invalid contrast. Check the user-provided contrast string.")
}
missing_groups <- setdiff(all.vars(parsed_expr), names(group_coef_list))
if (length(missing_groups) > 0) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = glue::glue("These groups in contrast do not exist in the design: {paste(missing_groups, collapse=', ')}"))
}
if (any(is.na(contrast_parser))) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = "Contrast vector contains NA — possible missing or empty group.")
}
# ---- 🧮 Dynamic Contrast (STANDARD METHOD) ----
design_vars <- all.vars(design(dds))
condition_col <- tail(design_vars, 1)
all_contrasts <- DESeq2::resultsNames(dds)
terms <- all.vars(as.formula(paste0("~", contrast)))
if (length(terms) == 2){
# --- SIMPLE PAIRWISE (e.g., A - B) ---
contrast_standard <- c(condition_col, terms[1], terms[2])
} else {
# --- COMPLEX INTERACTION ((A-B) - (C-D)) ---
contrast_standard <- contrast_parser
}
# ---- 🧬 Results & LFC Shrinkage ----
res_parser <- DESeq2::results(object               = dds,
contrast             = contrast_parser,
lfcThreshold         = lfc_cutoff,
altHypothesis        = "greaterAbs",
cooksCutoff          = TRUE,
independentFiltering = TRUE,
alpha                = padj_cutoff,
pAdjustMethod        = "BH")
# Use 'ashr' for shrinkage as it is robust for varied effect sizes
res_parser <- DESeq2::lfcShrink(dds = dds, res = res_parser, type = "ashr", quiet = TRUE)
#summary(res_parser)
summary(res_parser)
summary(res_parser_15)
summary(res_standard_ARCaPM)
res_standard <- res_standard_ARCaPM
# ---- 🔍 Detailed Comparison of Results ----
# 1. Convert to dataframes for comparison
df_p <- as.data.frame(res_parser)
df_s <- as.data.frame(res_standard)
# 2. Check rownames match
if (!all(rownames(df_p) == rownames(df_s))) {
stop("Row names do not match between Parser and Standard results!")
}
# 3. Check log2FoldChange within tight tolerance
tolerance <- 1e-8
lfc_diff <- abs(df_p$log2FoldChange - df_s$log2FoldChange)
lfc_is_equal <- all(lfc_diff < tolerance, na.rm = TRUE)
max_lfc_diff <- max(lfc_diff, na.rm = TRUE)
# 4. Check padj within tight tolerance
padj_diff <- abs(df_p$padj - df_s$padj)
padj_is_equal <- all(padj_diff < tolerance, na.rm = TRUE)
max_padj_diff <- max(padj_diff, na.rm = TRUE)
# 5. Compare DEG sets
deg_p <- rownames(df_p)[which(df_p$padj < padj_cutoff & abs(df_p$log2FoldChange) > lfc_cutoff)]
deg_s <- rownames(df_s)[which(df_s$padj < padj_cutoff & abs(df_s$log2FoldChange) > lfc_cutoff)]
shared_degs <- intersect(deg_p, deg_s)
only_parser <- setdiff(deg_p, deg_s)
only_standard <- setdiff(deg_s, deg_p)
# 6. Print Stringent Comparison Report
cat("\n--- ⚖️  Parser vs Standard Comparison Report (Stringent) ---\n")
cat("Numerical LFC Match: ", lfc_is_equal, " (Max Δ = ", formatC(max_lfc_diff, format='e', digits=2), ")\n", sep="")
cat("Numerical padj Match:", padj_is_equal, " (Max Δ = ", formatC(max_padj_diff, format='e', digits=2), ")\n", sep="")
cat("\nDEG Set Comparison:\n")
cat("- Shared DEGs:       ", length(shared_degs), "\n")
cat("- Only in Parser:    ", length(only_parser), "\n")
cat("- Only in Standard:  ", length(only_standard), "\n")
if (!lfc_is_equal | !padj_is_equal | length(only_parser) > 0 | length(only_standard) > 0) {
cat("\n⚠️  Warning: Differences detected! Check p-value or LFC precision.\n")
} else {
cat("\n✅ Success: Parser and Standard results are identical (stringent check).\n")
}
cat("-----------------------------------------------\n")
# Find the outlier genes driving max LFC diff
lfc_diff_vec <- abs(df_p$log2FoldChange - df_s$log2FoldChange)
outlier_genes <- rownames(df_p)[order(lfc_diff_vec, decreasing=TRUE)[1:10]]
df_p[outlier_genes, c("log2FoldChange", "padj")]
df_s[outlier_genes, c("log2FoldChange", "padj")]
# Check raw counts for worst offender
plotCounts(dds_parser, gene="ENSG00000293686", intgroup="Comparisons")
# Check standard errors before shrinkage
res_standard_unshrunk <- results(dds_standard,
contrast = contrast_vec,
alpha = 0.1)
res_standard_unshrunk["ENSG00000293686", c("log2FoldChange", "lfcSE", "pvalue")]
res_parser_unshrunk <- results(dds_parser,
contrast = contrast_parser_15,
alpha = 0.1)
res_parser_unshrunk["ENSG00000293686", c("log2FoldChange", "lfcSE", "pvalue")]
plotCounts(dds_parser, gene="ENSG00000293686", intgroup="Comparisons")
contrast_vec <- setNames(rep(0, length(resultsNames(dds_standard))),
resultsNames(dds_standard))
# Just the negative of ConditionWT.Treatment4Gy alone (22Rv1 is reference)
contrast_vec["ConditionWT.Treatment4Gy"] <- -1
res_standard_22Rv1 <- results(dds_standard, contrast = contrast_vec, alpha = 0.1)
res_standard_22Rv1 <- lfcShrink(dds_standard, res = res_standard_22Rv1,
type = "ashr", quiet = TRUE)
summary(res_standard_22Rv1)
contrast
contrast <- contrasts[16]
contrast
# For ashr
set.seed(1234)
# ---- 🧮 Dynamic Contrast Parsing (PARSER METHOD) ----
# Extract design variables (e.g., c("Condition", "Batch"))
# all.vars is robust; it handles ~Batch+Condition and ~Batch+Condition*Treatment
design_vars <- all.vars(DESeq2::design(dds))
# Extract the model matrix
# This is useful for downstream checking of rank deficiency
mod_mat <- stats::model.matrix(DESeq2::design(dds),
data = SummarizedExperiment::colData(dds))
# Create "Groups" column for easy subsetting/contrasts
if (length(design_vars) > 0) {
df_groups <- SummarizedExperiment::colData(dds) %>%
as.data.frame() %>%
tidyr::unite(col = "Groups", dplyr::all_of(design_vars), sep = "_", remove = FALSE)
# Update the dds colData to include this combined Groups column
SummarizedExperiment::colData(dds)$Groups <- as.factor(df_groups$Groups)
} else {
# If design is ~1, Groups is just "All"
df_groups <- SummarizedExperiment::colData(dds)$Groups <- as.factor("All")
}
# Define all possible groups that could be compared based on the design
groups <- unique(df_groups$Groups)
# Map groups to mean model coefficients (Get all possible coefficient vectors)
group_coef_list <- lapply(groups, function(i) colMeans(as.matrix(mod_mat[df_groups$Groups == i, , drop = FALSE])))
names(group_coef_list) <- groups
# Recursive function to evaluate the contrast string (e.g., "A-B") as a vector operation
replace_symbols <- function(node) {
if (is.symbol(node)) {
nm <- as.character(node)
if (nm %in% names(group_coef_list)) {
return(group_coef_list[[nm]])
} else {
# it's an operator like "-" or "+" → return unchanged
return(node)
}
} else if (is.call(node)) {
return(as.call(lapply(node, replace_symbols)))
} else {
return(node)
}
}
parsed_expr <- base::parse(text = contrast)[[1]]
expr_sub <- replace_symbols(parsed_expr)
contrast_parser <- base::eval(expr_sub)
# ⚠️ Sanity Check: contrast_parser Alignment
if (length(contrast_parser) != ncol(mod_mat)) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = glue::glue("Length of contrast_parser ({length(contrast_parser)}) does
not match the number of columns in the design matrix ({ncol(mod_mat)})."))
}
if (all(contrast_parser == 0)) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = "contrast_parser is all zeros — invalid contrast. Check the user-provided contrast string.")
}
missing_groups <- setdiff(all.vars(parsed_expr), names(group_coef_list))
if (length(missing_groups) > 0) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = glue::glue("These groups in contrast do not exist in the design: {paste(missing_groups, collapse=', ')}"))
}
if (any(is.na(contrast_parser))) {
log_error(sample = contrast,
step   = "extract_deseq2_results",
msg    = "Contrast vector contains NA — possible missing or empty group.")
}
# ---- 🧮 Dynamic Contrast (STANDARD METHOD) ----
design_vars <- all.vars(design(dds))
condition_col <- tail(design_vars, 1)
all_contrasts <- DESeq2::resultsNames(dds)
terms <- all.vars(as.formula(paste0("~", contrast)))
if (length(terms) == 2){
# --- SIMPLE PAIRWISE (e.g., A - B) ---
contrast_standard <- c(condition_col, terms[1], terms[2])
} else {
# --- COMPLEX INTERACTION ((A-B) - (C-D)) ---
contrast_standard <- contrast_parser
}
# ---- 🧬 Results & LFC Shrinkage ----
res_parser <- DESeq2::results(object               = dds,
contrast             = contrast_parser,
lfcThreshold         = lfc_cutoff,
altHypothesis        = "greaterAbs",
cooksCutoff          = TRUE,
independentFiltering = TRUE,
alpha                = padj_cutoff,
pAdjustMethod        = "BH")
# Use 'ashr' for shrinkage as it is robust for varied effect sizes
res_parser <- DESeq2::lfcShrink(dds = dds, res = res_parser, type = "ashr", quiet = TRUE)
summary(res_parser)
cor(res_parser$log2FoldChange, res_standard_22Rv1$log2FoldChange,
use = "complete.obs", method = "spearman")
